---
const { url, title, pubDate } = Astro.props;
---
<li class="p-4 list-none flex justify-between text-white bg-blue-400 hover:bg-blue-500 dark:bg-orange-400 dark:hover:bg-orange-500">
    <a href={url}>{title}</a>
    <p>{pubDate.slice(0, 10)}</p>
</li>
